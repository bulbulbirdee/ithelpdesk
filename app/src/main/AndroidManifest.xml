<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.example.ithelpdesk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Main2Activitydept"
            android:parentActivityName=".SplashActivity" />

        <activity
            android:name=".MainActivity"
            android:parentActivityName=".Main2Activitydept"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".TicketCategory"></activity>

        <activity
            android:name=".SubmitTicket"></activity>

        <activity
            android:name=".ComplaintDetail"></activity>

        <activity
            android:name=".Review"></activity>

        <activity
            android:name=".Subcategory"></activity>

    </application>

</manifest>